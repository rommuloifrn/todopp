{
    "name": "frontend",
    "private": true,

    "scripts": {
        "build": "tailwindcss -i ./main.css -o ../resources/static/main.css --watch"
      },

    "devDependencies": {
        "tailwindcss": "^3.4.3"
    }
}
